function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Personal Records Intelligence</p>
        <h1>React starter</h1>
        <p className="lede">
          This frontend is ready for the ingestion and query workflows we will
          build next.
        </p>
      </section>

      <section className="status-card">
        <h2>Starter status</h2>
        <p className="status-pill pending">Frontend scaffold ready</p>
      </section>
    </main>
  );
}

export default App;
